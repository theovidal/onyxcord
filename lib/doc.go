// Package lib contains all the core of the bot that makes it live :
// - Bot : a structure to hold configuration, commands and helper classes
// - Command : the model of a command, used in the commands package
// - Configuration : a structure to store the configuration, as defined in the according file (config.yml)
package lib
